import React from 'react'
import ReactDOM from 'react-dom/client'
import AppFront from '../Pages/AppFront'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppFront />
  </React.StrictMode>,
)
